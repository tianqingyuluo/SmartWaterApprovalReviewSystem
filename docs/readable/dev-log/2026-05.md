## 2026-05-26 - CP3-F 审核员初审动作与申请人结果回显

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-26-v1-cp3-reviewer-actions-applicant-result`
- Summary:
  - Java 后端新增审批人员初审动作接口，支持通过初审、退回补正和转人工复核三类 CP3 动作。
  - 任务主表新增最新处理快照，审批动作写入 `review_action_log` 操作日志，记录操作者、动作、状态、备注和时间。
  - 申请人结果、审批人员结果、任务状态和列表投影新增初审处理结果字段，供前端后续接入待办和详情动作。
  - 重复提交采用明确的 `409` 策略，并通过条件更新避免陈旧请求重复写入日志。
- Contract changes:
  - 新增 `POST /api/task/{taskId}/reviewer-action`。
  - 新增动作码 `APPROVE_INITIAL_REVIEW`、`RETURN_FOR_CORRECTION`、`TRANSFER_MANUAL_REVIEW`。
  - 新增处理状态 `INITIAL_REVIEW_PASSED`、`CORRECTION_REQUIRED`、`MANUAL_REVIEW_REQUIRED`。
  - 新增 `review_action_log` 表和 `review_task` 初审处理快照字段。
- Documentation updates:
  - `.trellis/spec/backend/smartwater-mvp-contracts.md`
  - `.trellis/spec/backend/database-guidelines.md`
  - `.trellis/spec/frontend/smartwater-mvp-visibility.md`
  - `docs/readable/modules/backend/api.md`
  - `docs/readable/modules/backend/database.md`
  - `docs/readable/modules/frontend/state-and-visibility.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `git diff --check` 通过。
  - `cd java-services/water-approval && ./mvnw test -DskipTests=false` 通过，86 tests。
- Known issues:
  - 本任务只完成初审动作和结果回显，不包含补正材料重传、材料版本差异和 Word 报告导出。
- Follow-ups:
  - CP3-C 前端接入待办、AI 结果、三类动作和操作日志展示。
  - CP3-D 补齐 Java-Python-前端全链路稳定性验收证据。

---

## 2026-05-26 - CP3-B Python FastAPI、规则与 Agent 主线

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-14-v1-cp3-python-fastapi-rules-agent`
- Summary:
  - Python 服务新增 FastAPI 审查任务入口，提供 `/health`、`POST /api/review/tasks` 和 `GET /api/review/tasks/{aiTaskId}`。
  - 新增 `ReviewTaskOrchestrator`，让 FastAPI 后台任务和轮询 Worker 共用下载/OCR/规则/RAG/Agent/回写主链路。
  - Agent 或 LLM 失败时降级为规则结果和人工复核提示，仍通过 Java 回写 `PARTIAL_SUCCESS`。
  - 审核推理失败日志改为记录失败分类和异常类型，不记录 `sessionId`、token 或原始供应商异常文本。
  - 增加 FastAPI、orchestrator、锁文件和类型检查相关测试与依赖。
- Contract changes:
  - 新增 `INTERNAL_API_TOKEN`，非空时 FastAPI 任务接口要求 `X-Internal-Token`。
  - FastAPI 任务 DTO wire contract 使用 camelCase，Python 内部使用 Pydantic alias 映射。
  - `pyproject.toml` / `uv.lock` 新增 FastAPI 与 uvicorn 依赖。
- Documentation updates:
  - `.trellis/spec/backend/smartwater-mvp-contracts.md`
  - `.trellis/spec/backend/review-reasoning-adapter.md`
  - `python-services/smart-water-approval-review-system-py/README.md`
  - `python-services/smart-water-approval-review-system-py/WORKER_API.md`
  - `docs/readable/modules/worker/api.md`
  - `docs/readable/modules/worker/adapters.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `uv run ruff check ...` 通过。
  - `uv run mypy src` 通过。
  - `uv run python -m compileall src main.py` 通过。
  - `uv lock --check` 通过。
  - `uv run pytest -q tests/test_fastapi_app.py tests/test_review_orchestrator.py tests/test_result_writer.py tests/test_review_adapter.py tests/test_ocr_adapter.py` 通过，20 tests。
- Known issues:
  - FastAPI 任务状态当前为进程内存储，重启后只保留 Java 侧权威状态和结果。
  - 本轮未执行真实 OCR/LLM/Java 联机调用，普通测试均使用 mock/stub。
- Follow-ups:
  - 继续执行 CP3-F 审核员初审动作与申请人结果回显，或接入 Java 主动调度 FastAPI 的集成验收。

---

## 2026-05-26 - CP3-E 最小 RBAC 与任务可见性

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-26-v1-cp3-minimal-rbac-task-visibility`
- Summary:
  - Java 后端新增最小登录、当前用户、种子账号、角色常量和 Sa-Token 拦截器。
  - 任务提交写入 `owner_user_id`，申请人、审批人员、管理员按角色获得不同任务可见性。
  - Worker 领取任务、状态更新、结果回写和材料下载继续走 `X-Worker-Token`，不混用网页登录态。
  - 前端接入真实登录 token，按角色控制菜单、默认入口、申请创建入口和结果查询投影。
  - 修正申请人结果链接误入审批人员结果流的问题，申请人不再请求 reviewer-only API。
- Contract changes:
  - 新增 `POST /api/auth/login` 与 `GET /api/auth/me`。
  - 普通业务 API 要求 `Authorization: Bearer <token>`。
  - 新增 `review_task.owner_user_id` 和 `user_account` 表。
  - `GET /api/task/{taskId}/result/reviewer` 限制为 `REVIEWER` / `ADMIN`。
- Documentation updates:
  - `.trellis/spec/backend/smartwater-mvp-contracts.md`
  - `.trellis/spec/backend/database-guidelines.md`
  - `.trellis/spec/frontend/smartwater-mvp-visibility.md`
  - `docs/readable/modules/backend/api.md`
  - `docs/readable/modules/backend/database.md`
  - `docs/readable/modules/frontend/state-and-visibility.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `cd java-services/water-approval && ./mvnw -Dmaven.repo.local=/tmp/m2 test` 通过，75 tests。
  - `cd frontend && npm run test` 通过，12 tests。
  - `cd frontend && npm run build` 通过。
- Known issues:
  - 当前种子账号只服务 CP3 本地演示；完整用户管理、角色分配页面留到 CP4。
  - `ReviewTaskServiceImpl` 中仍有 JSON Map/List 反序列化带来的 unchecked 编译告警，功能测试已覆盖关键路径。
- Follow-ups:
  - 继续执行 CP3-B Python FastAPI/规则/Agent 链路或 CP3-F 审核员动作与申请人结果回显。

---

## 2026-05-14 - CP1 证据整理（CP1-D）

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-14-v1-cp1-docs-git-demo-evidence`
- Summary:
  - 建立 CP1 评分点到 MVP 证据的映射总表，覆盖 CP1-A（Java 架构）、CP1-B（Python 服务）、CP1-C（前端页面）、CP1-D（Git/运行/答辩）全部评分项。
  - 标注每个评分项的当前证据状态："已有"直接引用现有文档/代码位置；"待补齐"明确缺口说明和后续负责子任务。
  - 整理本地启动步骤文档，覆盖 MySQL、MinIO/RustFS、Java 后端、Python Worker、前端五个服务的启动顺序、配置模板和健康检查方式。
  - 整理 Git 分工与提交证明，包含 4 名团队成员职责、提交统计、关键提交记录、分支策略和 PR 历史。
- Documentation updates:
  - `docs/readable/cp1-evidence.md` — CP1 评分点到 MVP 证据映射总表
  - `docs/readable/operations/local-dev.md` — 本地启动步骤
  - `docs/readable/cp1-git-evidence.md` — Git 分工与提交证明
  - `docs/readable/dev-log/2026-05.md` — 本条目
- Verification:
  - 文档通过代码目录与当前实现交叉核对，未改动运行时代码。
  - 所有文档链接使用相对路径，符合 `docs/readable/` 目录结构规范。
- Known issues:
  - 部分证据项（如数据库联通截图、前端运行截图、FastAPI 改造差距清单）标注为"待补齐"，由 CP1-A/B/C 子任务在 Wave 2 中分别完成。
- Follow-ups:
  - CP1-A 补齐 Java 架构运行截图和数据库/对象存储联通证据。
  - CP1-B 补齐 FastAPI 改造差距清单和 Python 依赖清单。
  - CP1-C 补齐前端运行截图和角色化改造说明。

---

## 2026-05-13 - 前端 Tailwind 样式体系重构

- Branch: `mvp/smartwater`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-13-frontend-tailwind-refactor`
- Summary:
  - 前端接入 TailwindCSS 3、PostCSS 和 Autoprefixer，新增 `tailwind.config.js` 与 `postcss.config.js`。
  - 将申请列表、新建申请、AI 初审结果三页以及核心通用/业务组件迁移为 Tailwind utility class 与少量 `sw-*` 共享 class。
  - 将主蓝、深蓝侧栏、浅灰背景、卡片边框/阴影、状态色、圆角和字体等后台管理界面 token 沉淀到 Tailwind theme。
  - `frontend/src/style.css` 收敛为 Tailwind 入口、基础全局规则和少量共享组件样式，不再承载大段页面级样式。
- Contract changes:
  - 无后端 API、任务状态、材料类型或请求/轮询语义变更。
  - 前端构建配置新增 Tailwind/PostCSS 处理链路。
- Documentation updates:
  - `.trellis/spec/frontend/styling-guidelines.md`
  - `.trellis/spec/frontend/index.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `cd frontend && npm run test` 通过，2 files / 8 tests。
  - `cd frontend && npm run build` 通过。
- Known issues:
  - 未做浏览器截图级像素验收；本轮依据设计稿方向、构建结果和人工 diff 检查确认不引入伪功能。
- Follow-ups:
  - 后续新增前端页面或组件时继续复用 Tailwind `sw` token，避免回到页面级大段 scoped CSS。

## 2026-05-13 - 前端审核流程重构与申请列表 API

- Branch: `mvp/smartwater`
- Related tasks:
  - `.trellis/tasks/05-12-frontend-review-flow-redesign`
- Summary:
  - 前端从双页演示升级为申请列表、新水务申请、AI 初审结果三页流程，并按 `docs/UI设计/` 中的设计稿重构后台管理界面。
  - Java 后端新增前端可用的申请列表 API，返回任务、会话、状态、时间、知识包版本和三类材料上传状态，不复用 Worker 内部抢占任务接口。
  - 前端结果映射补强模型失败、schema 错误、`BLOCKER` 和人工复核语义，避免 `COMPLETED` 被误展示为完全成功。
  - 请求层统一处理 Java `R<T>` 业务码，`code != 200` 会转为前端错误而不是被当作成功数据。
- UI changes:
  - 三页统一为深蓝侧栏、顶部工具栏、浅灰背景、白色业务卡片和蓝色主操作。
  - 申请列表只展示真实后端支撑字段：任务 ID、状态、提交/更新时间、知识包版本、材料提交情况和详情入口。
  - 新建申请页只提交 MVP 三类材料，页面明确申请信息字段当前为本地记录，不声称已经进入后端业务模型。
  - AI 初审结果页展示真实材料状态、字段抽取、问题清单、风险提示、审核意见和人工复核提示，不伪造 PDF/证照预览或不可用审批动作。
- Spec updates:
  - `.trellis/spec/frontend/quality-guidelines.md` 补充 Java `R<T>` 业务码处理规则和测试门禁。
- Verification:
  - `cd frontend && npm run test` 通过，2 files / 8 tests。
  - `cd frontend && npm run build` 通过。
  - `cd java-services/water-approval && ./mvnw test` 通过，55 tests。
  - `git diff --check` 通过。
- Known issues:
  - 本轮 UI 使用现有 scoped CSS 实现，尚未迁移到 TailwindCSS；后续另开专项任务处理依赖、配置和样式体系迁移。
  - 未做浏览器截图级像素验收。

## 2026-05-11 - SmartWater Worker OCR 切换到 GLM OCR

- Branch: `mvp/smartwater`
- Related tasks:
  - `.trellis/tasks/05-11-fix-ocr-model-selection`
- Summary:
  - Python Worker 的 OCR 适配器从旧的 `chat/completions` + `glm-4v` 路径切换到官方 GLM OCR 版面解析接口。
  - OCR 请求现在发送到 `POST {OCR_GLM_BASE_URL}/layout_parsing`，请求体使用 `model: "glm-ocr"` 和 `file`，并把 `md_results` / `layout_details` 映射回现有 `ExtractedField[]`。
  - `FieldExtractor` 和后续 Worker 流程保持不变，材料下载、字段补充和结果回写边界没有改动。
- Contract changes:
  - OCR 适配器不再依赖 `glm-4v` 或 `/chat/completions`。
  - GLM OCR 响应中的 `md_results` 优先转成单条 markdown 字段；缺少 `md_results` 时再回退到 `layout_details`。
  - 不支持的扩展名继续返回空抽取结果，避免把非 OCR 文件送进模型接口。
- Documentation updates:
  - `python-services/smart-water-approval-review-system-py/WORKER_API.md`
  - `python-services/smart-water-approval-review-system-py/.env.example`
  - `docs/readable/README.md`
  - `docs/readable/modules/worker/adapters.md`
  - `docs/readable/modules/worker/failure-handling.md`
  - `.trellis/spec/backend/index.md`
  - `.trellis/spec/backend/ocr-adapter.md`
- Verification:
  - `python -m compileall src main.py` 通过。
  - `python -m pytest -q` 在系统 Python 上不可用，因为该解释器未安装 `pytest`。
  - `uv run ruff check .` 通过。
  - `uv run mypy src knowledge_pack tests` 通过。
  - `uv run python -m pytest -q` 通过，18 passed。
- Known issues:
  - 未做真实 GLM OCR 联机调用，仅用 mock 响应验证官方接口形状。

## 2026-05-04 - SmartWater Worker MVP 合并前契约收尾

- Branch: `review/mvp-code-quality-fix`
- PR: #5
- Owner: `6newuser6`
- Related tasks:
  - `.trellis/tasks/04-24-smartwater-python-ocr-review-worker-mvp`
- Summary:
  - Python Worker 对 Java 统一 `R<T>` 响应补齐业务码校验，避免将 HTTP 200 但业务失败的响应误判为成功。
  - Worker 拉取待处理任务、更新状态、回写结果和下载材料统一携带 `X-Worker-Token`，并补充相关回归测试。
  - Java 结果查询对齐 `materialCompleteness.missing` 到 `missingMaterials` 投影，状态流转业务错误码与数据库初始化入口同步修正。
- Contract changes:
  - Worker 仅在 HTTP 成功且 `code == 200` 时将 JSON 接口视为成功。
  - `/material/download` 为二进制下载接口，继续按 HTTP 状态码判断成功与否。
  - 非法状态流转按业务错误返回 `400/409`，不再落入通用 `500`。
- Documentation updates:
  - `docs/readable/dev-log/2026-05.md`
  - `docs/readable/modules/worker/failure-handling.md`
- Verification:
  - `./mvnw test` 通过，46 tests。
  - `uv run python -m compileall main.py src tests` 通过。
  - `uv run python -m unittest discover -s tests -p test*.py` 通过，5 tests。
- Known issues:
  - `./mvnw test` 在普通沙箱内可能因本地 Maven 缓存目录权限或 Mockito inline agent attach 限制失败，提权环境可通过。
- Follow-ups:
  - PR #5 合并后继续进行 MVP 端到端联调与集成验收。

## 2026-05-04 - MVP 质量门禁与协作规范固化

- Branch: `task/project-readable-docs-spec`
- Related tasks:
  - `.trellis/tasks/04-24-smartwater-project-spec-hardening-mvp`
- Summary:
  - PR #5 合并后，将本轮 review 中暴露出的测试、密钥、RustFS、Worker 鉴权和跨服务契约问题沉淀到 `.trellis/spec/**`。
  - 后端质量规范升级为 `Active`，并加入 backend pre-development checklist。
  - 团队协作规范补充 PR Quality Gate 和选择性 TDD 纪律，要求 review bugfix 同步补回归测试。
- Spec updates:
  - Java 后端 PR 必须在 clean checkout 下通过 `./mvnw test`，普通测试不得依赖真实 MySQL、RustFS、OCR、LLM、外网或未提交密钥。
  - Python Worker PR 必须通过语法检查和可用单测，并 mock 后端 HTTP、OCR、LLM 和材料下载依赖。
  - RustFS 是本地集成/运行期对象存储目标，业务代码通过 S3-compatible `StorageService` 抽象访问；普通单测使用 mock 或 fake storage。
  - 状态流转、Worker `X-Worker-Token`、申请人/审批人员可见性、结果回写 schema 和 `missingMaterials` 映射属于必须测试的稳定契约。
- Verification:
  - 本次变更仅涉及 `.trellis/spec/**`、任务元数据和 `docs/readable/**` 文档。
  - 已确认 PR #5 最新 head `12e10ca` 在合并前通过 Java 46 tests、Python compileall 和 Python 5 tests。
- Follow-ups:
  - 前端双页 MVP、法规知识包和后续端到端联调稳定后，继续补齐对应目录、组件、API 使用和集成验收规范。

## 2026-05-05 - 法规知识包 MVP 契约对齐

- Branch: `task/smartwater-regulation-knowledge-pack-mvp`
- PR: #6
- Owner: `ymx545`
- Related tasks:
  - `.trellis/tasks/04-24-smartwater-regulation-knowledge-pack-mvp`
- Summary:
  - 增加取水许可 MVP 静态法规知识包，覆盖材料清单、申请书字段规则、审核依据、提示词片段和人工复核规则。
  - 将 PR6 知识包结构与 PR5 已合并的 Java 后端和 Python Worker 契约对齐。
  - Worker 将知识包 `version` 回写为 `knowledgePackVersion`，Java 后端保存到 `review_task.knowledge_pack_version`。
- Contract changes:
  - `knowledge_pack/water_permit_mvp.json` 是 MVP 知识包默认文件。
  - Worker 会把 `reviewBasis[]` 和 `promptSnippets[]` 归一化为 adapter 可消费的 `knowledgeFragments`。
  - 模型输出的 `basisRefs` 只能引用本次输入的知识片段 ID，不能编造法规名称、条款号或来源 ID。
  - Worker 与 Java 后端的 wire contract 使用 camelCase，例如 `resultSummary`、`applicantResult`、`reviewerResult`、`knowledgePackVersion`。
- Documentation updates:
  - `python-services/smart-water-approval-review-system-py/WORKER_API.md`
  - `docs/readable/dev-log/2026-05.md`
  - `docs/readable/modules/worker/failure-handling.md`
- Verification:
  - `uv run python -m unittest discover -s tests -v` 通过，15 tests。
  - `uv run python -m compileall main.py src tests knowledge_pack` 通过。
  - `./mvnw test` 通过，47 tests。
- Known issues:
  - `./mvnw test` 在普通沙箱内可能因 Mockito inline agent attach 限制失败，提权环境可通过。
  - 本地存在未跟踪的 `python-services/smart-water-approval-review-system-py/uv.lock`，未纳入 PR6。

## 2026-05-08 - SmartWater 项目规范固化收尾

- Branch: `task/project-readable-docs-spec`
- Related tasks:
  - `.trellis/tasks/04-24-smartwater-project-spec-hardening-mvp`
- Summary:
  - 将 `.trellis/spec/backend/**` 和 `.trellis/spec/frontend/**` 中仍停留在模板或不相干示例阶段的文档，对齐到当前已合并的 Java 服务、Python Worker 和前端双页实现。
  - 后端规范补齐了真实目录结构、数据库表与实体同步、`R<T>`/`BusinessException` 错误契约、Worker token 鉴权边界以及任务处理日志约束。
  - 前端规范补齐了双页目录结构、页面与组件边界、`usePolling` composable、Pinia 预装但未使用的状态管理现状，以及 `api/task.ts` 的 DTO→view model adapter 契约。
- Spec updates:
  - `backend/index.md` 中原先的 `Partial` 条目升级为基于现状的 `Active` 规范。
  - `frontend/index.md` 中原先的模板化目录、状态、类型、质量条目升级为实际约定。
  - `backend/quality-guidelines.md` 去掉了不再适用的 `service/.../impl` 模板描述。
  - `frontend/quality-guidelines.md` 改为当前真实门禁：`npm run test`、`npm run build` 和 adapter 回归测试。
- Verification:
  - 本次仅修改 `.trellis/spec/**`、任务元数据和 `docs/readable/dev-log/2026-05.md`。
  - 通过代码目录与当前实现交叉核对文档内容；未改动运行时代码，因此未重复执行应用测试。
- Follow-ups:
  - 代码与文档 review 通过后，可将 `smartwater-project-spec-hardening-mvp` 进入提交与归档流程。

## 2026-05-08 - SmartWater 总体设计父任务归档

- Branch: `task/project-readable-docs-spec`
- Related tasks:
  - `.trellis/tasks/04-23-smartwater-architecture-roadmap`
  - `.trellis/tasks/04-24-smartwater-backend-submission-storage-mvp`
  - `.trellis/tasks/04-24-smartwater-python-ocr-review-worker-mvp`
- Summary:
  - SmartWater 总体设计父任务已完成 MVP、V1、V2 路线规划、模块边界、跨模块流程和首批子任务拆分。
  - 父任务 PRD 中列出的 MVP 子任务已进入完成或归档状态，包括域模型契约、后端提交与存储、Python Worker、推理模型选型、前端双页、法规知识包和项目规范固化。
  - 本次收尾将仍处于 active 目录的后端提交与存储、Python Worker 以及总体设计父任务归档，保留任务 PRD 作为历史设计依据。
- Verification:
  - 归档前通过 `python3 ./.trellis/scripts/task.py list` 确认可见父任务为 `2/2 done`。
  - 归档前核对 `docs/readable/dev-log/2026-05.md` 已记录 Worker、法规知识包和项目规范固化等近期收尾记录。
  - 本次仅变更 Trellis 任务归档位置和可读开发日志，未改动运行时代码，因此未重复执行 Java、Python 或前端测试。
- Follow-ups:
  - 后续新功能、端到端联调或生产化改造应另开短任务分支和 Trellis 子任务，不再继续复用该父任务承载实现工作。

## 2026-05-18 - CP3-A Java AI 任务、回调与结果持久化

- Branch: `task/smartwater-regulation-knowledge-pack-mvp`
- PR: N/A
- Owner: `ymx545`
- Related tasks:
  - `.trellis/tasks/05-14-v1-cp3-java-ai-task-callback-results`
- Summary:
  - 补齐 Python Worker 到 Java 后端的字段快照回写链路，`reviewerResult.extractedFields` 现在携带 OCR / 字段抽取结果。
  - Java 服务层增加重复回调幂等回归测试，确认同一任务只保留 `APPLICANT` / `REVIEWER` 两类结果行并更新最新内容。
  - 审批人员结果查询现在通过测试锁定可返回字段快照；申请人结果不暴露字段快照。
- Contract changes:
  - Worker 回写结果时使用 camelCase `reviewerResult.extractedFields`。
  - `applicantResult` 不携带 `extractedFields`。
  - 结果回调按 `taskId + resultType` upsert，重复回调不新增重复结果。
- Documentation updates:
  - `.trellis/spec/backend/review-reasoning-adapter.md`
  - `.trellis/spec/backend/quality-guidelines.md`
  - `python-services/smart-water-approval-review-system-py/WORKER_API.md`
  - `docs/readable/modules/worker/api.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `java-services/water-approval`: `.\mvnw.cmd test` 通过，57 tests。
  - `python-services/smart-water-approval-review-system-py`: `.\.venv\Scripts\python.exe -m pytest` 通过，32 tests。
  - `python-services/smart-water-approval-review-system-py`: `.\.venv\Scripts\python.exe -m compileall src main.py knowledge_pack` 通过。
  - `python-services/smart-water-approval-review-system-py`: `.\.venv\Scripts\python.exe -m ruff check .` 通过。
  - `python-services/smart-water-approval-review-system-py`: `.\.venv\Scripts\python.exe -m mypy src main.py knowledge_pack` 通过。
- Known issues:
  - 本机未安装 `uv`，本次使用 Python 3.14 创建本地 `.venv` 验证 Python 测试与静态检查。
- Follow-ups:
  - CP3 后续联调任务可基于该回写契约接入 FastAPI 规则 Agent 与前端审批工作台。

---

## 2026-05-19 - CP2-A Java 配置与知识库触发入口

- Branch: `task/v1-cp2-java-ai-config-health`
- PR: N/A
- Owner: `ymx545`
- Related tasks:
  - `.trellis/tasks/05-14-v1-cp2-java-ai-config-health`
- Summary:
  - 新增 Java 侧 AI 服务配置 `water-approval.ai-service.*`，覆盖 Python AI/MCP base URL、健康检查路径、内部 token、超时、MCP transport/path 和 ingest CLI 参数。
  - 新增 `/api/ai/health`，用于 CP2 验收时查看 Java 配置并探活 Python HTTP 健康端点；Python 不可达时返回结构化降级信息，不影响 Java 服务可用性。
  - 新增 `/api/ai/ingest`，输出当前 Python ingest CLI 运维命令和 MCP demo 验证命令；当前不伪造 REST ingest 远程执行。
- Contract changes:
  - `AI_SERVICE_INTERNAL_TOKEN` 仅用于 Java 探活请求头 `X-Internal-Token`，响应和日志只暴露是否已配置。
  - Python ingest 当前权威入口仍是 `uv run python -m src.ingest.cli ...`，Java 侧入口模式标记为 `ops-command`。
- Documentation updates:
  - `java-services/water-approval/API.md`
  - `java-services/water-approval/CONFIG.md`
  - `docs/readable/operations/local-dev.md`
  - `.trellis/spec/backend/smartwater-mvp-contracts.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `java-services/water-approval`: `.\mvnw.cmd test` 通过，64 tests。
- Known issues:
  - 当前 Python MCP Server 原生 transport 与 ingest CLI 已存在，但没有正式 REST health/ingest 网关；`/api/ai/health` 的可达性取决于后续 Python HTTP 健康端点或适配服务是否启动。

---

## 2026-05-20 - CP2-D 文档、测试与演示脚本归档

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/05-14-v1-cp2-docs-tests-demo`
- Summary:
  - 新增 CP2 文档主入口，明确知识库资料白名单、非主线资料边界和“从空 ChromaDB 重建”步骤。
  - 补充 Embedding 配置口径：当前 Python ingest 使用 OpenAI SDK `embeddings.create`，支持 OpenAI-compatible embeddings 端点；当前示例更新为 `EMBEDDING_BASE_URL=https://router.tumuer.me/v1` 与 `EMBEDDING_MODEL=Qwen/Qwen3-Embedding-4B`，并明确 base URL 不能写成完整 `/v1/embeddings` 路径。
  - 新增 CP2 答辩演示脚本，串联 Java `/api/ai/health`、`/api/ai/ingest`、Python ingest 与 MCP 工具、前端 `/knowledge-mcp` 演示台流程。
  - 新增 CP2 测试与截图归档，记录主链路测试、常见异常验证，以及当前环境无法导出截图时的补图清单。
- Documentation updates:
  - `docs/readable/cp2-readme.md`
  - `docs/readable/cp2-demo-script.md`
  - `docs/readable/cp2-test-evidence.md`
  - `docs/readable/README.md`
  - `docs/readable/dev-log/2026-05.md`
- Verification:
  - `java-services/water-approval`: `./mvnw -q -Dtest=AiOpsControllerTest,AiOpsServiceTest,AiServicePropertiesTest test` 通过（`RC=0`）。
  - `python-services/smart-water-approval-review-system-py`: `UV_CACHE_DIR=/tmp/uv-cache uv run python -m pytest -q tests/test_mcp_demo.py tests/test_mcp_server_registration.py tests/test_mcp_tools.py tests/test_cp2_evidence.py tests/test_ingest_pipeline.py tests/test_chroma_store.py tests/test_document_parser.py tests/test_embedding_client.py tests/test_text_splitter.py` 通过（`44 passed`）。
  - `python-services/smart-water-approval-review-system-py`: `UV_CACHE_DIR=/tmp/uv-cache uv run python -m compileall src main.py knowledge_pack` 通过。
  - `python-services/smart-water-approval-review-system-py`: `UV_CACHE_DIR=/tmp/uv-cache uv run python -m src.mcp_server.demo --list-tools` 与 `--run-samples` 通过。
  - `python-services/smart-water-approval-review-system-py`: 使用 Router OpenAI-compatible Embedding 端点真实执行 `src.ingest.cli --rebuild` 通过，输出 `7` 个文档、`375` 个文本块、`665` 个 chunk、`665` 个向量。
  - `python-services/smart-water-approval-review-system-py`: `src.cp2_evidence --no-rebuild` 通过，验证 ChromaDB 向量总数 `665`，可检索“取水许可”“营业执照”“填报说明”“行业分类”，并演示 `knowledge_search`、`check_completeness`。
  - `frontend`: `npm run test` 通过（`8 tests`），`npm run build` 通过。
- Known issues:
  - 真实 ingest 重建仍依赖有效 `EMBEDDING_API_KEY` 与外网可达性；`.env` 不进入 Git。
  - 前端 `/knowledge-mcp` 当前为演示数据模式，后续接入 MCP HTTP 代理后可切换为真实链路。
