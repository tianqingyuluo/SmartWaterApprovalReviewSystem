# 2026-06 开发日志

## 2026-06-03 - CP4 Docker Compose 部署配置与 README

- Branch: `v1`
- PR: N/A
- Owner: `tianqingyuluo`
- Related tasks:
  - `.trellis/tasks/06-03-cp4-deploy-compose-readme`
- Summary:
  - 新增 `deploy/` 部署目录，集中放置 Docker Compose、部署环境变量模板、Java/Python/前端 Dockerfile、Nginx 反向代理配置和中文部署 README。
  - Compose 覆盖 MySQL、Redis、RustFS、Java 后端、Python FastAPI AI 服务和前端 Nginx，并为 MySQL、Redis、RustFS、ChromaDB 和知识库源目录配置持久化 volume。
  - Python 部署镜像安装 LibreOffice headless 与中文字体，用于 `.doc` 转换和中文材料解析。
  - 部署配置不依赖开发 `.env` 或 `application-secrets.yaml`，真实密码和模型密钥只进入 `deploy/.env`。
- Contract changes:
  - 新增部署配置契约：部署入口为 `deploy/docker-compose.yml`，模板为 `deploy/.env.example`；Java 通过 Compose 网络访问 `mysql`、`rustfs`、`python-ai`。
  - ChromaDB 继续是 Python 本地持久化目录，不引入独立 Chroma Server。
  - Redis 进入部署拓扑但当前仍是预留项，Java 登录态尚未接入 Redis。
- Documentation updates:
  - `.trellis/spec/backend/smartwater-mvp-contracts.md`
  - `deploy/README.md`
  - `docs/readable/operations/deployment.md`
  - `docs/readable/README.md`
  - `docs/readable/dev-log/2026-06.md`
- Verification:
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config --quiet` 通过。
  - `cd frontend && npm run build` 通过。
  - `cd python-services/smart-water-approval-review-system-py && uv run python -m compileall src main.py` 通过。
  - `cd java-services/water-approval && ./mvnw -Dmaven.repo.local=/tmp/m2-smartwater -B -DskipTests package` 通过。首次使用 `/tmp/m2-smartwater` 需要联网下载 Maven 依赖。
- Known issues:
  - Compose 静态配置可校验；完整启动仍依赖 Docker 镜像拉取、外部模型密钥和网络环境。
- Follow-ups:
  - 在可用部署主机上补一次完整 `docker compose up -d --build`、知识库 ingest、材料提交和结果查询联机证据。
